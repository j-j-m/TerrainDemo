# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def global_pods

pod 'BlueSocket'
end

target 'Controller' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Controller
global_pods
end

target 'terrainDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for terrainDemo
global_pods

  target 'terrainDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'terrainDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
